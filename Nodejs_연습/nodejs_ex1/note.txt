nodejs / javascript
기본적으로 비동기방식으로 처리
동기방식으로 처리하고 싶은경우,
1) 콜백함수
2) Promise
3) async 
3가지 방법 중 하나를 사용행서 처리해야한다.
아래순서일 수록 나중에 추가된 기술.

아래기술일수로 가독성이 개선됨.
async는 promise객체만 가능하므로 async사용하기 위해서는 promise를 먼저 선언해야한다.
